% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-market.R
\name{gen_candle_plots_with_sr_lines}
\alias{gen_candle_plots_with_sr_lines}
\title{Generate candlestick plot with support and resistance lines}
\usage{
gen_candle_plots_with_sr_lines(
  DT,
  support_pts,
  resistance_pts,
  near_frac = 0.01,
  label_digits = 2,
  show_ema_lines = FALSE,
  style = NULL,
  context = NULL
)
}
\arguments{
\item{DT}{A data.table containing OHLC data with columns \code{datetime}, \code{open}, \code{high}, \code{low}, and \code{close}.}

\item{support_pts}{Numeric vector of support price levels.}

\item{resistance_pts}{Numeric vector of resistance price levels.}

\item{near_frac}{Fraction of the displayed price range used to detect nearby
support/resistance labels that should be vertically separated.}

\item{label_digits}{Number of decimals used in support/resistance labels.}

\item{show_ema_lines}{Whether to draw EMA columns when \code{ema_20},
\code{ema_50}, \code{ema_100}, and \code{ema_200} exist in \code{DT}.}

\item{style}{Named style family or `NULL` to use the package default.}

\item{context}{Output context or `NULL` to use the package default.}
}
\value{
A ggplot2 object showing candlestick chart with support and resistance lines.
}
\description{
Generate candlestick plot with support and resistance lines
}
\examples{
bars <- data.table::data.table(
  datetime = as.POSIXct("2025-01-01", tz = "UTC") + 86400 * 0:9,
  open = 100 + (0:9), close = 100 + (0:9) + sin(0:9)
)
bars[, high := pmax(open, close) + 1]
bars[, low := pmin(open, close) - 1]
viz_candle_base(bars, show_compiler = FALSE)
gen_candle_plots_with_sr_lines(bars, support_pts = 99, resistance_pts = 110)
support <- data.table::data.table(zone_low = 98, zone_high = 100,
                                  zone_center = 99, score = 1)
resistance <- data.table::data.table(zone_low = 109, zone_high = 111,
                                     zone_center = 110, score = 1)
gen_candle_plots_with_sr_dts(bars, "Synthetic asset", "1D",
                            support_dt = support, resistance_dt = resistance,
                            show_compiler = FALSE)
denominator <- data.table::copy(bars)
denominator[, c("open", "high", "low", "close") := lapply(.SD, function(x) x / 2),
            .SDcols = c("open", "high", "low", "close")]
head(prep_ratio_ohlc(bars, denominator, "A/B"))
}
