% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-timeseries.R
\name{sim_arch1_dgp}
\alias{sim_arch1_dgp}
\title{Simulate an ARCH(1) data-generating process}
\usage{
sim_arch1_dgp(n, alpha0, alpha1, e_lag1_sq = 0, burn = 50, seed = NULL)
}
\arguments{
\item{n}{Number of observations returned after burn-in.}

\item{alpha0}{Constant term in the conditional variance recursion.}

\item{alpha1}{ARCH coefficient on the lagged squared innovation.}

\item{e_lag1_sq}{Initial lagged squared innovation.}

\item{burn}{Number of burn-in observations discarded before returning the sample.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
`data.table` with columns `t`, `value`, `innovation`, and `sigma2`.
}
\description{
Simulate an ARCH(1) data-generating process
}
\examples{
head(sim_ar1_dgp(30, a1 = 0.7, seed = 1))
head(sim_ar2_dgp(30, a1 = 0.5, a2 = 0.2, seed = 1))
head(sim_ar3_dgp(30, a1 = 0.4, a2 = 0.2, a3 = 0.1, seed = 1))
head(sim_ma1_dgp(30, b1 = 0.5, seed = 1))
head(sim_arch1_dgp(30, alpha0 = 0.1, alpha1 = 0.4, seed = 1))
head(sim_garch11_dgp(30, alpha0 = 0.1, alpha1 = 0.1, beta1 = 0.8, seed = 1))
}
