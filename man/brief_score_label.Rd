% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brief-score.R
\name{brief_score_label}
\alias{brief_score_label}
\title{Format the latest bounded research score}
\usage{
brief_score_label(score, moderate = 40, elevated = 70)
}
\arguments{
\item{score}{Numeric score on a 0 to 100 scale.}

\item{moderate}{Minimum score labeled moderate.}

\item{elevated}{Minimum score labeled elevated.}
}
\value{
A concise reader-facing character label.
}
\description{
Format the latest bounded research score
}
\examples{
prep_fill_forward(c(NA, 1, NA, 2))
a <- data.frame(date = as.Date("2025-01-01") + c(0, 2),
                value = c(4, 4.2), series = "A")
b <- data.frame(date = as.Date("2025-01-01") + c(1, 2),
                value = c(3, 3.1), series = "B")
prep_series_wide(list(a, b))
prep_zscore(c(1, 2, 3, 4))
prep_recent_changes(c(4, 4.1, 4.2, 4.15), lookback = 2)
score <- factor_bounded_score(prep_zscore(c(1, 2, 3, 4)))
brief_score_label(tail(score, 1))
}
