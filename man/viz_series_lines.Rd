% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-forecast.R
\name{viz_series_lines}
\alias{viz_series_lines}
\title{Plot one or more research series}
\usage{
viz_series_lines(
  data,
  x,
  y,
  color = "series",
  title,
  subtitle,
  y_label,
  colors = NULL,
  style = NULL,
  context = NULL,
  legend_position = "bottom",
  show_compiler = TRUE
)
}
\arguments{
\item{data}{Data frame containing the plotted columns.}

\item{x, y, color}{Column names mapped to x, y, and color.}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{y_label}{Y-axis label.}

\item{colors}{Optional named manual color vector.}

\item{style}{Named style family or `NULL` to use the package default.}

\item{context}{Output context or `NULL` to use the package default.}

\item{legend_position}{Legend position.}

\item{show_compiler}{Whether to append the configured compiler footer.}
}
\value{
A ggplot object.
}
\description{
Plot one or more research series
}
\examples{
# Fully synthetic inputs; no data service or local cache is needed.
changes <- seq(-0.10, 0.10, length.out = 40)
fan <- sim_forward_fan(4, changes, horizons = c(1, 5, 10),
                       n_paths = 100, seed = 7)
viz_forward_fan(fan, "Illustrative yield paths", "Historical-change bootstrap",
                "Yield (\%)", show_compiler = FALSE)
paths <- sim_scenario_path(4, c(Easing = -0.25, Hold = 0, Tightening = 0.25),
                           horizon_months = 3, start_date = as.Date("2025-01-01"))
viz_series_lines(paths, "date", "value", color = "scenario",
                 title = "Policy scenarios", subtitle = "Explicit monthly assumptions",
                 y_label = "Rate (\%)", show_compiler = FALSE)
}
