% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-style.R
\name{viz_annotate_event_lines}
\alias{viz_annotate_event_lines}
\title{Add event-line annotations to a plot}
\usage{
viz_annotate_event_lines(
  plot,
  event_dt,
  x_col = "datetime",
  label_col = NULL,
  color = NULL,
  linetype = "dashed",
  alpha = 0.45,
  style = NULL,
  context = NULL
)
}
\arguments{
\item{plot}{A `ggplot` object.}

\item{event_dt}{Data with at least an x-position column.}

\item{x_col}{Name of the x-position column.}

\item{label_col}{Optional label column.}

\item{color}{Optional line and label color.}

\item{linetype}{Line type for event markers.}

\item{alpha}{Alpha applied to event lines.}

\item{style}{Named style family or `NULL` to use the package default.}

\item{context}{Output context or `NULL` to use the package default.}
}
\value{
A `ggplot` object with event annotations.
}
\description{
Add event-line annotations to a plot
}
\examples{
viz_style_get("policy_memo", "report")
viz_palette_get("institutional_blue")
local({
  old <- options(investlabr.viz.style = "research_note", investlabr.viz.context = "report")
  on.exit(options(old))
  viz_style_set("research_note", "report")
})
data <- data.frame(date = as.Date("2025-01-01") + 0:9, value = 1:10)
p <- ggplot2::ggplot(data, ggplot2::aes(date, value)) + ggplot2::geom_line()
p <- viz_theme_apply(p, style = "policy_memo", show_compiler = FALSE)
p <- viz_annotate_last_value(p, data, "date", "value")
p <- viz_annotate_event_lines(p, data.frame(datetime = as.Date("2025-01-05")))
p <- viz_annotate_regime_bands(p, data.frame(
  start = as.Date("2025-01-02"), end = as.Date("2025-01-04")
))
viz_annotate_source_caption(p, "Synthetic data", "Illustration only")
}
