% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-event.R
\name{gen_plot_comparing_events}
\alias{gen_plot_comparing_events}
\title{Compare two event-study average paths}
\usage{
gen_plot_comparing_events(
  first_event_res,
  second_event_res,
  first_event_label,
  second_event_label,
  first_event_color = NULL,
  second_event_color = NULL,
  style = NULL,
  context = NULL
)
}
\arguments{
\item{first_event_res, second_event_res}{Event-performance results returned by
\code{eval_event_performance()}.}

\item{first_event_label, second_event_label}{Labels used in the comparison
legend.}

\item{first_event_color, second_event_color}{Optional line colors.}

\item{style}{Named style family or `NULL` to use the package default.}

\item{context}{Output context or `NULL` to use the package default.}
}
\value{
A ggplot object.
}
\description{
Compare two event-study average paths
}
\examples{
prices <- data.table::data.table(
  datetime = as.POSIXct("2025-01-01", tz = "UTC") + 86400 * 0:59,
  close = 100 * exp(0.001 * (0:59) + 0.01 * sin(0:59)),
  event_a = as.integer(seq_len(60) \%\% 10 == 0),
  event_b = as.integer(seq_len(60) \%\% 12 == 0)
)
attr(prices, "inst_id") <- "Synthetic asset"
a <- eval_event_performance(prices, "event_a", H = 1:5)
b <- eval_event_performance(prices, "event_b", H = 1:5)
gen_plot_event_tsline_cum_ret(a)
gen_plot_comparing_events(a, b, "Event A", "Event B")
}
