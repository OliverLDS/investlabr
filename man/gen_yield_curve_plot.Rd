% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factor-yieldcurve.R
\name{gen_yield_curve_plot}
\alias{gen_yield_curve_plot}
\title{Generate U.S. Treasury yield curve plot}
\usage{
gen_yield_curve_plot(
  DT,
  selected_windows = c("Now", "One week ago", "One month ago"),
  style = NULL,
  context = NULL
)
}
\arguments{
\item{DT}{A data.table returned from \code{get_yield_data_DT()}.}

\item{selected_windows}{Character vector specifying which yield curves to display (e.g., \code{"Now"}, \code{"One week ago"}).}

\item{style}{Named style family or `NULL` to use the package default.}

\item{context}{Output context or `NULL` to use the package default.}
}
\value{
A ggplot2 object visualizing selected yield curves.
}
\description{
Generate U.S. Treasury yield curve plot
}
\examples{
series <- lapply(seq_len(11), function(i) {
  data.table::data.table(date = as.Date(c("2025-01-01", "2025-02-01")),
                         value = c(4 + i / 20, 3.8 + i / 20))
})
curves <- get_yield_data_DT(series, list(
  Now = as.Date("2025-02-01"), Previous = as.Date("2025-01-01")
))
gen_yield_curve_plot(curves, selected_windows = c("Now", "Previous"))
gen_yield_curve_plot_grid(curves, selected_windows = c("Now", "Previous"))
}
